name: nginx

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: always
    networks:
      localnet:
        ipv4_address: 172.20.0.20
    volumes:
      - /etc/letsencrypt/:/etc/letsencrypt/
      - ./nginx/:/etc/nginx/
      - ./data/:/data/

    logging:
      driver: "local"
      options:
        max-size: "100m"

networks:
  localnet:
    external: true
